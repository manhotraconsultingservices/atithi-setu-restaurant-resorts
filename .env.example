# Database Configuration
# DB_TYPE can be 'sqlite' or 'postgres'
DB_TYPE=sqlite

# PostgreSQL Configuration (required if DB_TYPE=postgres)
PGHOST=
PGPORT=5432
PGUSER=
PGPASSWORD=
PGDATABASE=
PGSSL=false

# JWT Secret
JWT_SECRET=fallback-secret-atithi-setu-2024
