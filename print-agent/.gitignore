# Built binary (57 MB — distributed to clients directly, not via git)
dist/
# Local runtime config (contains the agent token)
.env
# pkg / npm scratch
node_modules/
# NSSM download scratch — keep only the small public-domain nssm.exe (bundled in the client package)
vendor/*
!vendor/nssm.exe
# runtime log written next to the service
agent.log
