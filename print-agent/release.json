{
  "latest": "3.5.0",
  "sha256": "e4d8deb82343d24f57fc5f99a786c4f1d563028e147764f00e9200b58960ecad",
  "url": "https://erp.atithi-setu.com/api/print-agent/download",
  "notes": "v3.5.0 — resilience: polling is decoupled from printing, so one slow/unreachable printer only backs up its own queue instead of freezing all printing; eager pre-warmed USB worker (no 1-3s first-ticket compile); 8s->2.5s network connect timeout; 200ms->30ms post-write flush. Pairs with server-side print-job retry backoff.",
  "_status": "ARMED 2026-09-03 — self-update is ON. Every agent on v3.3+ auto-updates to 3.5.0 within ~6h (download -> verify sha256 -> swap -> restart). The binary served by GET /api/print-agent/download is print-agent/dist/AtithiSetuPrintAgent.exe, force-committed so the VPS serves it. To pause self-update, set latest/sha256/url back to null. NOTE: prefer AGENT_EXE_PATH (VPS disk) or a CDN url for future releases to avoid committing the ~57MB binary to git history.",
  "_pending_release": {
    "version": "3.6.0",
    "reason": "P0+P1 latency. (P0, v3.5.1) SELF-HEAL the poll: agent clamps any configured POLL_MS>1000 back to 800ms (POLL_ALLOW_SLOW=1 to opt out) and installers/.env.example/README default to 800 not 3000 — re-tunes the ~2-3s stale-.env installs auto-update could never fix. (P1, v3.6.0) REAL-TIME PUSH: agent holds an SSE stream (GET /print-jobs/stream); server pushes a 'job' event the instant a ticket is enqueued → print in ~sub-300ms instead of a poll cycle; poll stays as a fallback (slow while streaming, fast when the stream drops); server hands jobs out via an ATOMIC CLAIM (PENDING→SENT lease, FOR UPDATE SKIP LOCKED) so a push+poll or two agents can't double-print. SERVER SIDE (claim + SSE endpoint + SENT-aware reconcile/health) deploys with the app and helps existing agents immediately (claim); the SSE endpoint is dormant until agents update.",
    "rollout": "Source + config are committed. To ship to the fleet: (1) build the exe — `cd print-agent && npx pkg . --targets node22-win-x64 --output dist/AtithiSetuPrintAgent.exe`; (2) compute its hash — `sha256sum dist/AtithiSetuPrintAgent.exe`; (3) set latest='3.5.1' and sha256=<that hash> here and commit the new dist exe (or, preferred, host it at AGENT_EXE_PATH/CDN and point url there). Agents on v3.3+ then auto-update within ~6h and self-heal their poll interval. Until then latest stays 3.5.0 so auto-update remains safe.",
    "instant_workaround": "To fix a specific tenant NOW without waiting for the build: on the billing PC edit the agent's .env to POLL_MS=800 and restart the AtithiSetuPrintAgent service (or re-run Setup.bat, which now defaults to 800)."
  },
  "_history": {
    "3.4.1": { "sha256": "e5479a152ab1c20647023f619486d1fe9e6d974ff87f90c4ca4ba287ee78f2cd", "note": "faster printing (persistent USB worker, shorter poll)" },
    "3.4.0": { "sha256": "6b4b49f643a8e8e2cec680be2c54efa33bd7a15120ad2d6ae07a83f66c497c46", "note": "superseded by 3.4.1 before any agent picked it up" },
    "3.3.0": { "sha256": "07c9b5dace9272bd56cb4a35b42da78056a075e4c6b0d5ca9fb5aa5a3dea670c" }
  }
}
